# RC15 FIX2

Removed from the 1.8 V2 reader UI and touch logic:
- `Satz` / sentence-jump control
- `Sperren` / reader-lock control

All other RC15 features remain unchanged.

The same two controls are intentionally excluded from the planned 3.49B V2 port.
