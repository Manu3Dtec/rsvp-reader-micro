# RC15 FIX1

Build fix:
- Added the ESP-IDF `esp_timer` component to the `main` component requirements.
- Required by `#include "esp_timer.h"` used for auto-sleep idle timing.
- No functional feature changes from RC15.
